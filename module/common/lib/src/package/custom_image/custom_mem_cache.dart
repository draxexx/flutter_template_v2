/// custom mem cache for using network image
final class CustomMemCache {
  /// [width] and [height] is using for image
  const CustomMemCache({required this.width, required this.height});

  /// mem cache width
  final int width;

  /// mem cache height
  final int height;
}
